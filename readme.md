- To use playlist capabilities, oAuth credentials are required, get your oAuth credentials from google developer console, it will look something like the example in `config/google-auth-example.json`

- Run `npm start`

- Login with the same email that you have provided in test user list.